<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI Bayan Chat — English with AI Bayan 4grade II</title>
<style>
  body {
    font-family: Verdana, sans-serif;
    background: linear-gradient(180deg,#fff6e5 0%,#f3e0b8 100%);
    margin: 0;
    color: #002b5c;
    text-align: center;
  }

  /* 🟡 HEADER */
  header {
    background: #003366;
    color: white;
    padding: 1rem;
    border-bottom: 5px solid gold;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.3rem;
  }

  /* 🔸 ЛОГОТИП B */
  .logo-circle {
    background: radial-gradient(circle at 30% 30%, gold 0%, #f4c542 40%, #003366 85%);
    color: white;
    font-weight: bold;
    font-size: 2.2rem;
    border-radius: 50%;
    width: 70px; height: 70px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 3px solid gold;
    box-shadow: 0 0 12px rgba(255,215,0,0.8);
    animation: pulse 2.5s infinite ease-in-out;
  }

  @keyframes pulse {
    0%,100% {transform: scale(1); box-shadow: 0 0 10px gold;}
    50% {transform: scale(1.08); box-shadow: 0 0 20px #ffd700;}
  }

  #chatBox {
    max-width: 650px;
    margin: 20px auto;
    background: white;
    border: 3px solid gold;
    border-radius: 15px;
    padding: 10px;
    height: 420px;
    overflow-y: auto;
    text-align: left;
    box-shadow: 0 0 10px rgba(0,0,0,0.2);
  }

  .msg {
    margin: 8px 0;
    padding: 10px 14px;
    border-radius: 10px;
    line-height: 1.4;
    max-width: 80%;
    animation: fadeIn 0.3s ease;
  }

  .user {
    background: #e3f2fd;
    border-left: 4px solid #1565c0;
    align-self: flex-end;
  }

  .ai {
    background: #fff8e1;
    border-left: 4px solid gold;
  }

  #inputArea {
    margin-top: 10px;
  }

  #userInput {
    width: 65%;
    padding: 10px;
    border-radius: 8px;
    border: 2px solid #003366;
    font-size: 1rem;
  }

  #sendBtn {
    padding: 10px 18px;
    border-radius: 8px;
    border: none;
    background: #003366;
    color: white;
    font-weight: bold;
    cursor: pointer;
  }

  #sendBtn:hover {
    background: gold;
    color: #003366;
  }

  @keyframes fadeIn {
    from {opacity: 0; transform: translateY(10px);}
    to {opacity: 1; transform: translateY(0);}
  }
</style>
</head>
<body>
  <!-- 🟡 ШАПКА С ЛОГОТИПОМ -->
  <header>
    <div class="logo-circle">B</div>
    <h1>🤖 AI Bayan Chat</h1>
    <p>English with <span style="color:gold;">AI Bayan</span> — Grade 4 II</p>
  </header>

  <main>
    <div id="chatBox">
      <div class="msg ai"><b>AI Bayan:</b> Hello! 🌟 I’m your English helper.  
      Ask me about grammar, translation or homework!</div>
    </div>

    <div id="inputArea">
      <input id="userInput" type="text" placeholder="Type your message..." />
      <button id="sendBtn" onclick="sendMessage()">Send</button>
    </div>
  </main>

<script>
async function askAIbayan(messageText) {
  try {
    const response = await fetch("https://shiny-tree-f78c.aiko030500.workers.dev", {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        model: "gpt-4.1-mini",
        messages: [
          { role: "system", content: "You are AI Bayan, a friendly English tutor for 4th grade students. Answer shortly and clearly in English." },
          { role: "user", content: messageText }
        ]
      })
    });

    if (!response.ok) throw new Error("Server error " + response.status);
    const data = await response.json();
    return data.choices?.[0]?.message?.content || "⚠️ No reply received from AI Bayan.";
  } catch (err) {
    console.error(err);
    return "❌ Connection error or invalid response.";
  }
}

async function sendMessage() {
  const input = document.getElementById("userInput");
  const message = input.value.trim();
  if (!message) return;

  const chatBox = document.getElementById("chatBox");
  chatBox.innerHTML += `<div class="msg user">${message}</div>`;
  input.value = "";

  chatBox.innerHTML += `<div class="msg ai">⏳ AI Bayan is thinking...</div>`;
  chatBox.scrollTop = chatBox.scrollHeight;

  const answer = await askAIbayan(message);
  chatBox.lastElementChild.innerHTML = `<b>AI Bayan:</b> ${answer}`;
  chatBox.scrollTop = chatBox.scrollHeight;
}
</script>
</body>
</html>
